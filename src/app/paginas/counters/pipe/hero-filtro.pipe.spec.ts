import { HeroFiltroPipe } from './hero-filtro.pipe';

describe('HeroFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
